import 'package:flutter/widgets.dart';

class InputLaunch extends StatelessWidget {
  const InputLaunch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Input Launch"),
    );
  }
}